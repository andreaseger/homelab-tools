{
  "name": "api-portal",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/api-portal",
  "tags": ["npm:private", "platform:amd64", "platform:arm64"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun --hot server/index.ts",
        "cwd": "{projectRoot}",
        "env": {
          "SPECS_DIR": "../../specs/"
        }
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun tsc --noEmit",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t api-portal --build-context workspace=. -f apps/api-portal/Dockerfile apps/api-portal",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
